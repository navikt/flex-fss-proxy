AZURE_APP_PRE_AUTHORIZED_APPS: "[{\"name\":\"dev-gcp:flex:flex-reisetilskudd-gsak\",\"clientId\":\"gsak-client-id\"}]"
pdl.url: http://pdl
syketilfelle.url: http://syketilfelle
security-token-service-token.url: http://sts

logging.config: "classpath:logback-local.xml"

serviceuser:
  username: "uname"
  password: "pass"

no.nav.security.jwt:
  issuer:
    aad:
      discoveryurl: http://localhost:${mock-oauth2-server.port}/aad/.well-known/openid-configuration
      accepted_audience: flex-fss-proxy
  client:
    registration:
      rest-sts:
        token-endpoint-url: http://localhost:${mock-oauth2-server.port}/sts/token
        grant-type: client_credentials
        authentication:
          client-id: "USER"
          client-secret: "PWD"
          client-auth-method: client_secret_basic
